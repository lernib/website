version: 0.0
os: linux
files:
  - source: /aws
    destination: /usr/local/codedeployresources
hooks:
  BeforeInstall:
    - location: aws/scripts/before-install.sh
      timeout: 300
      runas: root
    - location: aws/scripts/before-install-nopriv.sh
      timeout: 300
      runas: ec2-user
  AfterInstall:
    - location: aws/scripts/after-install.sh
      timeout: 300
      runas: root
    - location: aws/scripts/after-install-nopriv.sh
      timeout: 300
      runas: ec2-user